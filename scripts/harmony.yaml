# data:
#   activity: 'Freeview'

harmony_activity:
  alias: Harmony Activity
  sequence:
    service: remote.turn_on
    data_template:
      entity_id: remote.living_room
      activity: "{{ activity }}"
