notify_household:
# data:
#   title: "some title"
#   message: "some message"
  alias: Notify Household
  sequence:
    - service: notify.household
      data_template:
        title: "{{ title }}"
        message: "{{ message }}"
