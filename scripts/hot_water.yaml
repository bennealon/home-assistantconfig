hot_water_boost:
  # data:
  #   duration_hour: 1
  sequence:
    - service: ifttt.trigger
      data_template:
        event: "hot_water_on"
        value1: "{{ duration_hour }}"
    - service: script.notify_household
      data_template:
        title: "Hot Water"
        message: "Boosted for {{duration_hour }} hour."
