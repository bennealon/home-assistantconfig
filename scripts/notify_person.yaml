# data:
#   device: "ios_bens_iphone"
#   title: "some title"
#   message: "some message"

sequence:
  - service_template: notify.{{ device }}
    data_template:
      title: "{{ title }}"
      message: "{{ message }}"
      data:
        push:
          thread-id: "{{ title }}-notification-group"
